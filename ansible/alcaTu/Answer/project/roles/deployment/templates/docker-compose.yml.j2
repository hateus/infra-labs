version: "3.8"

services:
  {{ service }}:
    build: .
    restart: always
    ports:
      - {{ port }}:{{ port }}
    env_file:
      - .env

networks:
  default: 
    external: true
    name: {{ docker_network }}
